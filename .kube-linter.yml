checks:
  addAllBuiltIn: true
  exclude:
    - minimum-three-replicas
    - non-isolated-pod
    - required-annotation-email
    - required-label-owner
    - no-liveness-probe
    - no-readiness-probe
    - dnsconfig-options
